# v0.9.0 — 3 카테고리 × Top 10 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 단일 추천도 Top 10 → 종합/딸깍/자본X 카테고리별 Top 10 × 3 메시지 broadcast 모델로 전환. 자본 deploy hard exclude 해제, -1별 경감점으로 대체. row 끝 양성 태그.

**Architecture:** prompt가 카테고리 사이에 `===CATEGORY_SPLIT===` separator 출력 → `telegram_post.post()`가 separator로 1차 split + 4096자 2차 split → 순차 sendMessage. cache에는 통합본 저장 (separator 포함). prompt가 모든 ranking/tagging 책임 (LLM 자율 추정).

**Tech Stack:** Python 3.12 (subprocess, requests), pytest, ruff, Claude CLI subprocess. Spec: `docs/specs/2026-05-23-v0.9-three-categories.md`.

---

## File Structure

- **Modify** `src/airdropbot/telegram_post.py`: Add `_split_by_separator(text, sep)` helper. Modify `post(text)` to 1차 separator split → 2차 4096자 split → 순차 send.
- **Modify** `tests/test_telegram_post.py`: Add tests for separator split + post() integration.
- **Modify** `prompts/airdrop_digest.md` (대규모): 헤더 v0.9 + §3.2 -1별 경감점 + §4 hard exclude 제거 + §5 3 카테고리 + separator + 태그 + §6 Skipped 룰 갱신. 라벨 "공식"→"링크".
- **Modify** `prompts/airdrop_pin.md`: snapshot_md 라벨 "공식"→"링크". 헤더 v0.9 bump.

---

## Task 1: telegram_post — separator split helper (TDD)

**Files:**
- Modify: `src/airdropbot/telegram_post.py:15-41` (add helper)
- Test: `tests/test_telegram_post.py` (add test class)

- [ ] **Step 1.1: Write failing test for separator split (single separator)**

Append to `tests/test_telegram_post.py`:

```python
def test_split_by_separator_single():
    from airdropbot.telegram_post import _split_by_separator
    text = "part1\n\n===CATEGORY_SPLIT===\n\npart2"
    result = _split_by_separator(text, "===CATEGORY_SPLIT===")
    assert result == ["part1", "part2"]


def test_split_by_separator_multiple():
    from airdropbot.telegram_post import _split_by_separator
    text = "a\n\n===CATEGORY_SPLIT===\n\nb\n\n===CATEGORY_SPLIT===\n\nc"
    result = _split_by_separator(text, "===CATEGORY_SPLIT===")
    assert result == ["a", "b", "c"]


def test_split_by_separator_absent_returns_single_chunk():
    from airdropbot.telegram_post import _split_by_separator
    text = "no separator here"
    result = _split_by_separator(text, "===CATEGORY_SPLIT===")
    assert result == ["no separator here"]


def test_split_by_separator_strips_surrounding_whitespace():
    """separator 앞뒤 공백·개행 정리. chunk 자체 trim()."""
    from airdropbot.telegram_post import _split_by_separator
    text = "  part1  \n\n===CATEGORY_SPLIT===\n\n  part2  "
    result = _split_by_separator(text, "===CATEGORY_SPLIT===")
    assert result == ["part1", "part2"]


def test_split_by_separator_skips_empty_chunks():
    """연속 separator나 leading/trailing separator로 인한 빈 chunk 제거."""
    from airdropbot.telegram_post import _split_by_separator
    text = "===CATEGORY_SPLIT===\na\n===CATEGORY_SPLIT===\n===CATEGORY_SPLIT===\nb\n===CATEGORY_SPLIT==="
    result = _split_by_separator(text, "===CATEGORY_SPLIT===")
    assert result == ["a", "b"]
```

- [ ] **Step 1.2: Run tests to verify they fail**

```bash
cd /home/ljk9121/projects/airdropbot
.venv/bin/python -m pytest tests/test_telegram_post.py -k "split_by_separator" -v
```

Expected: 5 FAIL with `ImportError: cannot import name '_split_by_separator'`.

- [ ] **Step 1.3: Implement `_split_by_separator`**

Add to `src/airdropbot/telegram_post.py` after `_split_message`:

```python
CATEGORY_SEPARATOR: Final[str] = "===CATEGORY_SPLIT==="


def _split_by_separator(text: str, separator: str = CATEGORY_SEPARATOR) -> list[str]:
    """카테고리 separator 기준 1차 split. 각 chunk는 strip(), 빈 chunk 제거.

    - separator 없으면 [text.strip()] 반환 (단일 chunk).
    - separator로 나뉜 chunk 중 strip 후 빈 것은 제외.
    """
    parts = text.split(separator)
    return [p.strip() for p in parts if p.strip()]
```

- [ ] **Step 1.4: Run tests to verify they pass**

```bash
.venv/bin/python -m pytest tests/test_telegram_post.py -k "split_by_separator" -v
```

Expected: 5 PASS.

---

## Task 2: telegram_post — post() integrates separator + 4096 split (TDD)

**Files:**
- Modify: `src/airdropbot/telegram_post.py:72-85` (modify `post`)
- Test: `tests/test_telegram_post.py`

- [ ] **Step 2.1: Write failing tests for post() separator integration**

Append to `tests/test_telegram_post.py`:

```python
def test_post_splits_by_separator_then_sends_each_chunk(monkeypatch):
    """post()가 separator 기준 분리 후 각 chunk를 별도 sendMessage 호출."""
    from airdropbot import telegram_post

    monkeypatch.setenv("TELEGRAM_BOT_TOKEN", "T")
    monkeypatch.setenv("TELEGRAM_CHANNEL_ID", "@x")

    text = "category1\n\n===CATEGORY_SPLIT===\n\ncategory2\n\n===CATEGORY_SPLIT===\n\ncategory3"

    with patch("airdropbot.telegram_post.requests.post") as mock_post, \
         patch("airdropbot.telegram_post.time.sleep") as mock_sleep:
        mock_post.return_value = _FakeResp(200, '{"ok":true}')
        telegram_post.post(text)
        assert mock_post.call_count == 3
        sent_texts = [c[1]["json"]["text"] for c in mock_post.call_args_list]
        assert sent_texts == ["category1", "category2", "category3"]
        # 첫 chunk 외 chunk 사이 1초 delay 2회
        assert mock_sleep.call_count == 2


def test_post_falls_back_to_size_split_when_no_separator(monkeypatch):
    """separator 없으면 전체를 단일 카테고리로 보고 4096자 split만 적용."""
    from airdropbot import telegram_post

    monkeypatch.setenv("TELEGRAM_BOT_TOKEN", "T")
    monkeypatch.setenv("TELEGRAM_CHANNEL_ID", "@x")

    text = "short message no separator"

    with patch("airdropbot.telegram_post.requests.post") as mock_post:
        mock_post.return_value = _FakeResp(200, '{"ok":true}')
        telegram_post.post(text)
        assert mock_post.call_count == 1
        assert mock_post.call_args_list[0][1]["json"]["text"] == "short message no separator"


def test_post_splits_oversized_category_chunk_within_4096(monkeypatch):
    """단일 카테고리 chunk가 4096자 초과면 추가 split."""
    from airdropbot import telegram_post

    monkeypatch.setenv("TELEGRAM_BOT_TOKEN", "T")
    monkeypatch.setenv("TELEGRAM_CHANNEL_ID", "@x")

    big = ("line\n\n" * 1000).strip()  # ~6000 chars, separator 없음
    text = f"small\n\n===CATEGORY_SPLIT===\n\n{big}"

    with patch("airdropbot.telegram_post.requests.post") as mock_post, \
         patch("airdropbot.telegram_post.time.sleep"):
        mock_post.return_value = _FakeResp(200, '{"ok":true}')
        telegram_post.post(text)
        # 첫 chunk(small) 1개 + big이 2개로 split = 총 3개
        assert mock_post.call_count >= 2
        sent_texts = [c[1]["json"]["text"] for c in mock_post.call_args_list]
        assert sent_texts[0] == "small"
        # big chunk들은 모두 4096 이하
        for t in sent_texts[1:]:
            assert len(t) <= 4096
```

- [ ] **Step 2.2: Run tests to verify they fail**

```bash
.venv/bin/python -m pytest tests/test_telegram_post.py -k "post_" -v
```

Expected: 3 FAIL (현재 `post()`는 separator 모름).

- [ ] **Step 2.3: Modify `post()` to integrate separator split**

Replace `src/airdropbot/telegram_post.py:72-85` `post` 함수:

```python
def post(text: str) -> None:
    """Telegram 채널에 text post.

    1차 split: `===CATEGORY_SPLIT===` separator 기준 카테고리별 분리.
    2차 split: 각 카테고리 chunk가 4096자 초과 시 추가 split.
    chunk 사이 1초 delay (separator 경계든 4096자 경계든 동일).
    """
    token = os.environ.get("TELEGRAM_BOT_TOKEN")
    chat_id = os.environ.get("TELEGRAM_CHANNEL_ID")
    if not token:
        raise RuntimeError("TELEGRAM_BOT_TOKEN env 미설정")
    if not chat_id:
        raise RuntimeError("TELEGRAM_CHANNEL_ID env 미설정")

    categories = _split_by_separator(text)
    all_chunks: list[str] = []
    for cat in categories:
        all_chunks.extend(_split_message(cat))

    for i, chunk in enumerate(all_chunks):
        if i > 0:
            time.sleep(CHUNK_DELAY_SEC)
        _send_chunk(token=token, chat_id=chat_id, text=chunk)
```

- [ ] **Step 2.4: Run all telegram_post tests to verify**

```bash
.venv/bin/python -m pytest tests/test_telegram_post.py -v
```

Expected: 모든 테스트 PASS (기존 + 신규).

- [ ] **Step 2.5: ruff check**

```bash
.venv/bin/ruff check src/airdropbot/telegram_post.py tests/test_telegram_post.py
```

Expected: `All checks passed!`

- [ ] **Step 2.6: Stage (커밋은 Task 9 통합 commit에서)**

```bash
git add src/airdropbot/telegram_post.py tests/test_telegram_post.py
```

---

## Task 3: prompts/airdrop_digest.md — 헤더 + 사용자 프로필 update

**Files:**
- Modify: `prompts/airdrop_digest.md:1-13`

- [ ] **Step 3.1: 헤더 v0.8.0 → v0.9.0 + 사용자 프로필 + 변경 요약 교체**

Replace 라인 1-13 `prompts/airdrop_digest.md`:

```markdown
# airdrop-digest routine prompt (v0.9.0)

당신은 `/airdrop` routine입니다. 사용자 프로필: **자본 비쌈·시간 자유**. 자본 deploy 회피가 기본 선호지만, 자본 항목도 종합 ranking에 포함 (-1별 경감). testnet/social/quest 같은 저자본 활동 우선.

> **v0.9 변경 요약** (자세히는 `docs/specs/2026-05-23-v0.9-three-categories.md`):
> - 단일 Top 10 → **3 카테고리 × Top 10** (종합 / 딸깍 / 자본X), 중복 허용. 카테고리 사이에 `===CATEGORY_SPLIT===` separator.
> - 자본 deploy **hard exclude 해제**. 모든 listing 항목이 후보, 자본 deploy 항목은 시그널 별점 -1별 경감.
> - row 끝 양성 태그 `[딸깍]` (소요 ≤10분) `[자본X]` (deposit/swap/stake/매수 0원). 둘 다 만족 → `[딸깍][자본X]`, 둘 다 아님 → 태그 없음.
> - 라벨 변경: `공식` → `링크`.
>
> **v0.8 (이전) 요약** (참고: `docs/specs/2026-05-22-v0.8-html-links.md`):
> - HTML parse_mode. `<a href="...">링크</a> · <a href="...">출처</a> — ...` hyperlink. 2-pass detail enrichment.
```

- [ ] **Step 3.2: 결과 확인 (Read)**

```bash
# 1-15 라인 읽고 위 내용과 일치하는지 확인
```

---

## Task 4: prompts/airdrop_digest.md — §3.2 별점 산정에 -1별 경감점 추가

**Files:**
- Modify: `prompts/airdrop_digest.md` §3.2 부근

- [ ] **Step 4.1: §3.2 현재 내용 확인**

```bash
.venv/bin/python -c "print(open('prompts/airdrop_digest.md').read())" | sed -n '60,100p'
```

§3 또는 §3.2 (별점 산정) 위치 파악. 자본 deploy 가산점/감점 룰 명시 안 됐던 부분 찾기.

- [ ] **Step 4.2: -1별 경감점 룰 추가**

§3 별점 산정 룰 끝부분에 다음 줄 추가:

```markdown
- **자본 deploy 경감점 (v0.9)**: 위 시그널 합산 별점을 산정한 다음, 항목이 **자본 deploy 요구**(deposit/swap/stake/매수, gas 제외)면 **별 1개 차감** (최저 ★ ☆☆☆☆). gas 소액(<$5)은 자본 아님.
```

- [ ] **Step 4.3: 결과 확인 (Read)**

추가된 줄이 §3 마지막 별점 룰 다음에 있는지, §4 시작 전에 있는지 확인.

---

## Task 5: prompts/airdrop_digest.md — §4 hard exclude 제거

**Files:**
- Modify: `prompts/airdrop_digest.md` §4 부근 (dedupe 또는 hard exclude 명시 부분)

- [ ] **Step 5.1: hard exclude 문구 찾아 제거**

§4 또는 §3 내 "자본 deploy 요구 → 즉시 제외 (hard exclude)" 같은 문장 검색 후 다음으로 교체:

```markdown
### 4. 자본 deploy 정책 (v0.9 변경)

이전(v0.8까지): 자본 deploy 요구 항목은 listing 단계에서 즉시 제외 (hard exclude).

**v0.9**: hard exclude 해제. 모든 listing 항목이 후보로 진입. 자본 deploy 정보(deposit/swap/stake/매수 액수, gas 제외)는 §3 별점 산정 시 -1별 경감점 + §5.2 `[자본X]` 태그 부착 여부 판정에 사용.

자본 deploy 항목은 §6 Skipped 섹션에 더 이상 자동 기록하지 않는다 (모두 후보 풀에 진입).
```

기존 §4 (dedupe) 내용은 §4.1로 강등하거나 다른 번호로 재배치. dedupe 핵심 룰은 보존:

```markdown
### 4.1 dedupe (기존 룰 유지)

여러 사이트에 같은 프로젝트가 등장하면 정보 가장 풍부한 항목 하나로 합치고, 출처는 cross-link로 모두 표기. dedupe 끝나면 카테고리별 top 10 후보 선정 (§5.2).
```

- [ ] **Step 5.2: 결과 확인 (Read)**

§4 전체 새 구조 확인. detail enrichment(§4.5 → 새 §5.x로?) 위치도 점검 — 본질은 detail enrichment가 카테고리별 top 10 union dedupe 후 실행이라는 점.

---

## Task 6: prompts/airdrop_digest.md — §5 3 카테고리 출력 + separator + 태그

**Files:**
- Modify: `prompts/airdrop_digest.md` §5 (출력)

이 task가 가장 큰 변경. §5 전체 rewrite.

- [ ] **Step 6.1: §5 전체 교체**

§5 (출력) 부터 §5.1.1 (HTML 안전 가드)까지 전체를 다음으로 교체:

```markdown
### 5. 출력 (한국어 HTML, 3 카테고리 × Top 10)

> **중요**: Telegram은 `parse_mode=HTML`로 메시지를 보낸다 (v0.8). 따라서:
> - **허용되는 유일한 HTML 태그**: `<a href="URL">label</a>`. 그 외(`<b>`, `<i>`, `<code>` 등) 모두 금지.
> - **markdown 금지**: `**bold**`, `### header`, `---` — Telegram이 해석 안 함.
> - **HTML 이스케이프**: 본문 텍스트 안에 `&` `<` `>` 문자가 있으면 각각 `&amp;` `&lt;` `&gt;`로 치환. URL 자체(href 속성값)는 원본 그대로.
> - **카테고리 separator**: 종합/딸깍/자본X 카테고리 사이에 단독 라인으로 `===CATEGORY_SPLIT===`. telegram_post가 이걸 인식해 3개 메시지로 분리.

#### 5.1 전체 구조

```
[종합 카테고리]
(핀 섹션, 활성 핀 ≥1개일 때만)
🪂 오늘의 에어드랍 — 종합 Top 10 (YYYY-MM-DD)
(row × 10)
─────────────
Skipped / excluded (§6)

===CATEGORY_SPLIT===

[딸깍 카테고리]
⚡ 딸깍 Top 10 (10분 이내)
(row × 10)

===CATEGORY_SPLIT===

[자본X 카테고리]
💸 자본X Top 10 (자본 0)
(row × 10)
```

- separator(`===CATEGORY_SPLIT===`)는 단독 라인. 앞뒤 빈 줄 둘.
- Skipped 섹션은 종합 카테고리 메시지에만 노출 (운영 정보).

#### 5.2 카테고리별 필터·정렬

| 카테고리 | 필터 | 정렬 |
|---|---|---|
| **종합** | 없음 (전체 후보) | 별점(§3) 내림차순 |
| **딸깍** | 예상 소요 ≤ 10분 (회수 무관 — one-time/daily/weekly OK) | 별점 내림차순 |
| **자본X** | deposit/swap/stake/매수 0원 (gas <$5 OK) | 별점 내림차순 |

각 카테고리 Top 10. **중복 허용** — 한 프로젝트가 종합/딸깍/자본X 모두 등장 가능.

후보 부족(<10) 시 있는 만큼 출력 + 마지막에 `(후보 N개)` 한 줄.

#### 5.3 핀 섹션 (종합 카테고리 prefix)

활성 핀 ≥1개일 때만 종합 메시지 맨 앞:

```
📌 Pinned (<N>개)

(각 핀의 snapshot_md 박제, 줄바꿈 1개로 구분)

─────────────

🪂 오늘의 에어드랍 — 종합 Top 10 ...
```

핀 0개면 위 섹션 생략, 곧바로 `🪂` 헤더부터.

딸깍/자본X 카테고리에는 핀 미노출.

#### 5.4 Row 포맷

예시 (자본 0 + 짧은 활동):
```
<a href="ACT_URL">링크</a> · <a href="SRC_URL">출처</a> — Nexus(NEXUS) - testnet faucet 클릭 (~5분)
추천도: ★★★★☆ [딸깍][자본X]
```

예시 (자본 deploy + 긴 활동, 태그 없음):
```
<a href="ACT_URL">링크</a> · <a href="SRC_URL">출처</a> — Foo(FOO) - $30 stake 후 weekly quest 수행
추천도: ★★☆☆☆
```

예시 (공식 URL 미확보 fallback):
```
<a href="SRC_URL">출처</a> — Bar(티커 미정) - 어디 가서 무엇을 클릭
추천도: ★★★☆☆ [자본X]
```

#### 5.5 포맷 규칙

- **링크 묶음 (공식 URL 확보)**: `<a href="ACT_URL">링크</a> · <a href="SRC_URL">출처</a>` — 라벨 "**링크**" (v0.9 변경, v0.8까지 "공식"). 사이 ` · ` (공백 + U+00B7 middle dot + 공백).
- **링크 묶음 (공식 URL 미확보, fallback)**: `<a href="SRC_URL">출처</a>` 단독. `링크 ·` 부분 생략.
- **링크 묶음과 프로젝트명 사이**: ` — ` (공백 + em dash + 공백).
- **프로젝트명(티커)**: 페이지 명시 시 `Nexus(NEXUS)`, 미정이면 `Nexus(티커 미정)`.
- **프로젝트명과 할 일 사이**: ` - ` (공백 + hyphen + 공백).
- **할 일**: 한 줄. 자본 deploy 액수는 본문에 포함 (예: `$30 stake`). 줄바꿈 금지.
- **추천도 라인**: `추천도: ` + 별 5칸(★/☆) + 태그(있을 때만 공백 1개 후).
- **번호 prefix 금지**: `1.`, `2.` 같은 번호 절대 안 됨.
- **항목 간 빈 줄 1개**.

#### 5.6 태그 부착 룰 (v0.9 신설)

- `[딸깍]`: 예상 소요 시간 **≤ 10분** (one-time/daily/weekly 무관). 페이지에 시간 명시되면 그대로, 미명시 시 활동 복잡도로 합리적 추정. 정보 부족 시 미부착 (false negative 허용).
- `[자본X]`: **deposit/swap/stake/매수 0원** (gas 소액 <$5는 자본 아님 — testnet faucet, social quest, waitlist 등 해당). 정보 부족 시 미부착.
- 둘 다 만족 → `[딸깍][자본X]` (공백 없이 연결). 둘 다 아님 → 태그 없음.
- **순서 고정**: [딸깍] 먼저, [자본X] 나중.
- **추천도-태그 간격**: 공백 1개. 예: `추천도: ★★★★☆ [딸깍][자본X]`.

#### 5.7 카테고리별 dedupe

종합 카테고리 내에서만 활성 핀 dedupe 적용 (v0.8과 동일 룰 — 토큰 단위 일치). 딸깍/자본X 카테고리는 핀 dedupe 없음 (사용자가 카테고리별 신선한 ranking 보고 싶음).

종합/딸깍/자본X 카테고리 **간** 중복은 의도된 것 — dedupe 안 함.

#### 5.8 HTML 안전 가드

- `<a>` 태그는 반드시 짝이 맞아야 함 — 열고 닫기. 짝 안 맞으면 Telegram 400.
- `href` 속성값에 `"` 들어가는 URL은 거의 없으나, 있으면 해당 row를 fallback(출처 단독)으로.
- 텍스트 영역에 `<a>` 외 다른 HTML 태그 등장 시 제거.
```

- [ ] **Step 6.2: 결과 확인 (Read)**

§5 전체 구조 + separator 출현 + 태그 룰이 명시됐는지 확인.

---

## Task 7: prompts/airdrop_digest.md — §6 Skipped 룰 갱신

**Files:**
- Modify: `prompts/airdrop_digest.md` §6

- [ ] **Step 7.1: Skipped 섹션 자본 hard exclude 항목 제거**

§6 (Skipped/excluded) 내 "자본 deploy 요구 (hard exclude)" 항목 제거 (이제 후보임). 새로운 §6:

```markdown
### 6. Skipped/excluded 섹션 (종합 카테고리 메시지에만)

종합 카테고리 Top 10 row 다음에:

```
─────────────
Skipped / excluded
- fetch 실패: <url> (사유: 403 / timeout / ...)
- 공식 URL 추출 실패 (detail page): <프로젝트명> — 출처만 노출
- ROI 미달 제외: <프로젝트명> — <한 줄 사유>
- 📌 만료 자동 정리: <N>개 — <project1, project2, ...>     (정리한 게 있을 때만)
- ⚠️ pinned.yaml 파싱 실패 — 핀 섹션 생략됨. 수동 점검 필요.    (YAML 손상 시)
- ⚠️ <id> invalid — skip                                          (일부 레코드만 invalid 시)
```

**v0.9 변경**: "자본 deploy 요구 (hard exclude)" 항목 제거 — 자본 deploy 항목은 이제 종합 ranking에 포함 (별점 -1별 경감) + [자본X] 태그 부재로 식별.

Skipped 섹션은 **종합 카테고리 메시지에만** 노출. 딸깍/자본X 메시지에는 미노출 (이 두 카테고리는 filter+sort만, 운영 정보 무관).
```

- [ ] **Step 7.2: 결과 확인 (Read)**

§6에서 "hard exclude" 사라지고 v0.9 변경 메모 추가됐는지 확인.

---

## Task 8: prompts/airdrop_pin.md — snapshot 라벨 + 헤더 bump

**Files:**
- Modify: `prompts/airdrop_pin.md:1` (헤더) + snapshot_md 예시 부분

- [ ] **Step 8.1: 헤더 v0.8.0 → v0.9.0**

Replace 라인 1:

```markdown
# airdrop-pin routine prompt (v0.9.0)
```

- [ ] **Step 8.2: snapshot_md 라벨 "공식" → "링크"**

snapshot_md 예시들에서:

이전:
```yaml
snapshot_md: |
  <a href="<공식_URL>">공식</a> · <a href="<출처_URL>">출처</a> — <프로젝트명>(<티커>) - <할 일 한 줄>
```

이후:
```yaml
snapshot_md: |
  <a href="<공식_URL>">링크</a> · <a href="<출처_URL>">출처</a> — <프로젝트명>(<티커>) - <할 일 한 줄>
```

또 fallback (공식 URL 없는 경우):
이전:
```yaml
snapshot_md: |
  <a href="<출처_URL>">출처</a> — <프로젝트명>(<티커>) - <할 일 한 줄>
```
(변경 없음, 라벨 "출처" 유지)

문서 본문 텍스트(설명) 안의 "공식 URL" 같은 표현은 그대로 유지 가능 — 의미상 "프로젝트 공식 사이트의 URL"이라서. 단 row 라벨 자체는 모두 "링크"로.

snapshot_md 정규화 룰(§6 안):
이전: "broadcast row에서 `<a href="...">공식</a>` 태그가 있으면..."
이후: "broadcast row에서 `<a href="...">링크</a>` 태그가 있으면..."

- [ ] **Step 8.3: 태그 부착 안 함 명시 (v0.9 신설)**

snapshot_md 정규화 룰 끝에 다음 추가:

```markdown
> 7. **태그 미부착 (v0.9)**: broadcast row의 `[딸깍][자본X]` 태그는 snapshot_md에 박제하지 않는다. 시간 지나면 태그 정확도(특히 [딸깍]의 시간 추정) 떨어지므로 추천도와 동일하게 핀에는 미포함.
```

- [ ] **Step 8.4: 결과 확인 (Read)**

라벨 "공식" → "링크"로 모두 변경, 헤더 v0.9, 태그 미부착 룰 명시됐는지.

---

## Task 9: pytest + ruff 통과 + 통합 commit 준비

**Files:**
- 검증만

- [ ] **Step 9.1: pytest 실행**

```bash
.venv/bin/python -m pytest -v
```

Expected: 모든 테스트 PASS (기존 + 신규 separator 테스트).

- [ ] **Step 9.2: ruff 검사**

```bash
.venv/bin/ruff check .
```

Expected: `All checks passed!`

- [ ] **Step 9.3: 변경 파일 검토**

```bash
git status
git diff --stat
```

Expected modified: `src/airdropbot/telegram_post.py`, `tests/test_telegram_post.py`, `prompts/airdrop_digest.md`, `prompts/airdrop_pin.md`. 신규: `docs/specs/2026-05-23-v0.9-three-categories.md`, `docs/plans/2026-05-23-v0.9-three-categories.md`.

(v0.8 잔여 변경분 — telegram_post.py HTML parse_mode, prompts v0.8 — 도 미커밋 상태일 수 있음. v0.9 commit에 포함 가능 또는 별도 v0.8 commit 분리 결정은 사용자에게 확인.)

- [ ] **Step 9.4: 사용자에게 commit 분리 전략 확인**

질문: "v0.8 미커밋분(HTML parse_mode + 2-pass enrichment + airdrops.io /visit/ 보강)과 v0.9 변경분을 (a) 단일 commit으로 묶을지, (b) v0.8 / v0.9 별도 commit으로 분리할지?"

사용자 답에 따라 진행.

---

## Task 10: Smoke test (사용자 manual)

**Files:**
- 실행만

- [ ] **Step 10.1: 사용자에게 daily routine 실행 안내**

```bash
cd ~/projects/airdropbot
set -a && source .env && set +a
.venv/bin/python -m airdropbot.daily 2>&1 | tee -a logs/daily-$(date +%F).log
```

소요 ~7-8분 예상. 사용자 DM에 3개 메시지(종합/딸깍/자본X) 도달.

- [ ] **Step 10.2: 결과 검증 체크리스트**

도달한 메시지 + `cache/latest-digest.md` 확인:
- [ ] 3 메시지 모두 도달 (종합 → 딸깍 → 자본X 순서)
- [ ] 각 메시지 단독으로 4096자 이내
- [ ] separator `===CATEGORY_SPLIT===` 메시지에 노출 안 됨 (split 후 버려짐)
- [ ] 각 카테고리 Top 10 (또는 후보 부족 시 명시)
- [ ] row 라벨이 "공식"이 아닌 "링크"
- [ ] 태그 [딸깍] [자본X] 부착 — 시간/자본 조건에 맞게
- [ ] 자본 deploy 항목이 종합 top에 등장 + [자본X] 부재로 식별 가능
- [ ] 핀 0개라 종합 메시지 prefix 핀 섹션 없음 + 곧바로 🪂 헤더

- [ ] **Step 10.3: 결과 분석 & 보고**

각 항목 PASS/FAIL 표 + 발견된 prompt 추정 오류·이상 (예: 자본 deploy 액수 정보 부정확, [딸깍] 잘못 부착 등) 정리. v0.9.1 보강이 필요한 부분이 있으면 사용자에게 보고.

---

## Task 11: NEXT.md 갱신 + 통합 commit (사용자 승인 후)

**Files:**
- Modify: `NEXT.md` (v0.9.0 상태 반영)
- Stage 전체 변경분

- [ ] **Step 11.1: NEXT.md v0.9.0 상태로 갱신**

`NEXT.md`의 "현재 상태" 섹션을 v0.7.0 → v0.9.0. 변경:
- HEAD commit 갱신 (commit 후 채움)
- 구현 완료 항목 추가: v0.8 HTML 링크 + v0.8.4 detail enrichment 보강 + v0.9 3 카테고리
- routine 평균 시간 ~3분 → ~7-8분 (v0.9 후보 풀 확대)
- 직전 commit 흐름 갱신

- [ ] **Step 11.2: 사용자에게 commit 메시지 초안 제시 후 승인 받기**

초안:
```
v0.9.0: 3 카테고리 × Top 10 + 자본 deploy ranking 포함

- 종합/딸깍/자본X 카테고리별 Top 10, Telegram 3 메시지 분리 (separator: ===CATEGORY_SPLIT===)
- 자본 deploy hard exclude 해제 → 별점 -1별 경감점으로 대체
- row 끝 양성 태그: [딸깍] (≤10분) / [자본X] (자본 0)
- 라벨: 공식 → 링크
- prompts/airdrop_digest.md, airdrop_pin.md 헤더 v0.9.0 bump
- telegram_post.py: separator 1차 split + 4096자 2차 split (5 신규 테스트)

Spec: docs/specs/2026-05-23-v0.9-three-categories.md
Plan: docs/plans/2026-05-23-v0.9-three-categories.md
```

- [ ] **Step 11.3: 사용자 명시 승인 후 commit**

global CLAUDE.md "커밋 자율 금지" 룰. 사용자가 명시적으로 "commit해라" 또는 "OK 진행" 답한 후에만 실행.

```bash
git add prompts/airdrop_digest.md prompts/airdrop_pin.md src/airdropbot/telegram_post.py tests/test_telegram_post.py docs/specs/2026-05-23-v0.9-three-categories.md docs/plans/2026-05-23-v0.9-three-categories.md NEXT.md
git commit -m "$(cat <<'EOF'
v0.9.0: 3 카테고리 × Top 10 + 자본 deploy ranking 포함

- 종합/딸깍/자본X 카테고리별 Top 10, Telegram 3 메시지 분리 (separator: ===CATEGORY_SPLIT===)
- 자본 deploy hard exclude 해제 → 별점 -1별 경감점으로 대체
- row 끝 양성 태그: [딸깍] (≤10분) / [자본X] (자본 0)
- 라벨: 공식 → 링크
- prompts/airdrop_digest.md, airdrop_pin.md 헤더 v0.9.0 bump
- telegram_post.py: separator 1차 split + 4096자 2차 split (5 신규 테스트)

Spec: docs/specs/2026-05-23-v0.9-three-categories.md
Plan: docs/plans/2026-05-23-v0.9-three-categories.md

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```

- [ ] **Step 11.4: git push 별도 확인**

global rule: push도 명시 요청해야. 사용자가 push 요청 안 했으면 commit만 하고 멈춤.

---

## Self-Review

**Spec coverage** (spec § → plan task):
- §1 출력 모델: Task 6 §5.1-5.2 ✅
- §2 후보 풀 + 추천도: Task 4 (별점 -1) + Task 5 (hard exclude 제거) ✅
- §3 태그 정의: Task 6 §5.6 ✅
- §4 Row 포맷: Task 6 §5.4-5.5 ✅
- §5 메시지 split: Task 1 + Task 2 (telegram_post separator) + Task 6 §5 separator instruction ✅
- §6 핀 섹션 위치: Task 6 §5.3 ✅
- §7 detail enrichment 비용 변화: spec note만, 코드 변경 없음 (운영 관찰)
- §8 Pin snapshot 포맷: Task 8 ✅
- §9 사용자 프로필: Task 3 (헤더 문구) ✅

**Placeholder scan**: TBD/TODO 없음. 모든 step에 구체 코드/명령. ✅

**Type consistency**: 
- `_split_by_separator(text, separator)` Task 1 정의 → Task 2 사용. ✅
- `CATEGORY_SEPARATOR` constant Task 1 정의. ✅
- `===CATEGORY_SPLIT===` 문자열 일관 (Task 1/2/6/spec). ✅

**Note on Task granularity**: Task 6 (§5 전체 rewrite)이 2-5분 한도 초과 가능. 큰 task지만 spec 본질이 §5에 집약되어 split 강제하면 중간 상태가 깨짐. 단일 atomic edit로 유지.

---

## Execution Handoff

**Plan complete and saved to `docs/plans/2026-05-23-v0.9-three-categories.md`. Two execution options:**

1. **Subagent-Driven (recommended)** — 각 task별로 fresh subagent 디스패치, task 사이 review, 빠른 iteration.
2. **Inline Execution** — 현 세션에서 executing-plans skill로 batch 실행 + checkpoint 사이 review.

이 프로젝트는 한 사람(사용자) + 한 메인 컨텍스트에서 운영되고, prompt 변경이 LLM 추정 결과를 보는 게 검증의 본질이라 inline execution이 더 효율. 그러나 task 12개라 main context 부담 가능 — subagent도 OK.

**Which approach?**
